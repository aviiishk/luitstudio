export const metadata = {
  title: "Blog — Luit Studio",
  description: "Insights, tutorials and updates from the Luit Studio team.",
};

export default function BlogLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
